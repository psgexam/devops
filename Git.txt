git config --global user.name "urname"
git config --global user.email "urmail"
git config --list
mkdir devopslab
cd 
git clone
git status
git add hello.py
git status
git commit -m "First Commit"
git remote set-url origin "your likn"
git push origin main
